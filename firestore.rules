rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to access data by sync code
    // Sync codes act as shared keys for cross-device access
    match /tinyWins/{syncCode} {
      allow read, write: if request.auth != null;
    }
  }
}
